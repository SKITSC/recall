version: "3"

services:
    web:
        build:
            context: ./
            dockerfile: web.Dockerfile
        ports:
            - "80:80"
        depends_on: 
            - db
        restart: always
    db:
        build:
            context: ./
            dockerfile: db.Dockerfile
        ports:
        - "3306:3306"
        restart: always